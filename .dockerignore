# 縮小 build context，加快 docker build

# Maven 產出（build 階段會重新編譯）
target/

# 上傳檔（執行期由 volume 掛入，不進 image）
uploads/

# 機密（絕不進 image）
.env

# 版本控制 / IDE
.git/
.gitignore
.settings/
.project
.classpath
.factorypath
.idea/
*.iml
.vscode/

# 文件 / 其它
HELP.md
*.md
docker-compose.yml
