document.addEventListener('DOMContentLoaded',()=>{const toast=document.querySelector('.toast');const notify=m=>{toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)};document.querySelectorAll('[data-preview-tab]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-preview-tab]').forEach(x=>x.classList.remove('active'));button.classList.add('active');document.querySelectorAll('[data-preview-panel]').forEach(panel=>panel.hidden=panel.dataset.previewPanel!==button.dataset.previewTab)}));document.querySelector('[data-preview-submit]').addEventListener('click',()=>{const checks=[...document.querySelectorAll('.preview-check input')];if(checks.some(x=>!x.checked)){notify('請完成所有送審前確認項目');return}notify('課程已送出審核，狀態更新為審核中')})});
